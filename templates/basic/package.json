{
  "name": "@templates/basic",
  "description": "McFly starter project",
  "scripts": {
    "start": "mcfly serve",
    "prepare": "mcfly prepare",
    "dev": "mcfly serve",
    "build": "mcfly build",
    "preview": "node .output/server/index.mjs",
    "build:preview": "npm run build && npm run preview"
  },
  "dependencies": {
    "@mcflyjs/cli": "^0.0.26",
    "@mcflyjs/config": "^0.1.4",
    "@mcflyjs/core": "^0.5.3",
    "nitropack": "~2.8"
  }
}
